{
  "name": "whatsapphub",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "setup": "chmod +x setup-server.sh && ./setup-server.sh"
  },
  "keywords": ["hubspot", "oauth", "integration", "crm"],
  "author": "",
  "license": "ISC",
  "description": "Integración con HubSpot usando OAuth",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^4.18.2"
  }
}